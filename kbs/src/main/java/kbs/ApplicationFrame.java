package kbs;

import javax.swing.*;

public class ApplicationFrame extends JFrame {
    ApplicationFrame(){
        setSize(500, 500);
        setLocationRelativeTo(null);

        setVisible(true);
    }
}
